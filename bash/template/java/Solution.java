
package $(padded_ordinal)_$(snaked_problem);

class Solution {

    ${returning} ${method} (${params}) {
        return solution (...);
    }

    private ${boxing} solution (...) {
        throw new UnsupportedOperationException ();
    }

}
